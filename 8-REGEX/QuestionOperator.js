let vascao = /\S?/
console.log(vascao.test(""))

//este operador é utilizador para definir que o simbolo anterior se torne opcional na string, caso esteja presente, a regex se torna true caso seja false.