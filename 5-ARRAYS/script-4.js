let numeros = [1, 6, 9];
for (let i = 0; i <= numeros.length; i++) {
  console.log(numeros[i]);
}

// ENQUANTO O COMPRIMENTO DO ARRAY FOR MAIOR QUE 0, ELE IMPRIME OS SEUS INDICES E O 0 GANHA +1 A CADA VEZ QUE A CONDIÇÃO FOR TRUE, SE FOR FALSE, PARA.

let users = ["pedro", "vasco"];
for (let i = 0; i <= users.length; i++) {
  console.log(users[i]);
}
